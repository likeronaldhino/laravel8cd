pipeline {
    agent any
    stages {
        stage("Build")
        {
            environment
            {
                DB_HOST = credentials("balman-host")

            }

            steps {
                sh 'ls'
                sh 'echo $DB_HOST'

            }
        }

    }
}
