pipeline {
    agent any
    stages {
        stage("Build")
        {
            environment
            {
                DB_HOST = credentials("laravel-host")

            }

            steps {
                sh 'ls'
                sh 'echo $DB_HOST'

            }
        }

    }
}
